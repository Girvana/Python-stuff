#tests to see if variable is string or not -Girvana
def testForString(string):
    try:
        float(string)
        return True
    except ValueError:
        return False
        
#makes sure variable only contains alphabetical characters -Girvana
while not variable.isalpha():
  print("Invalid input")
  variable=("input variable: ")
